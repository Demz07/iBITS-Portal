[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd 'iBITS Portal'",
  "dotnet restore",
  "dotnet publish -c Release -o out"
]

[start]
cmd = "cd 'iBITS Portal/out' && dotnet 'iBITS Portal.dll'"
