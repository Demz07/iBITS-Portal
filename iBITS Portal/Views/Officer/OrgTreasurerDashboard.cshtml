@* C:\Users\Dave\OneDrive\Desktop\copies of code bases from the iBITS Portal\OrgTreasurerDashboard.cshtml *@
@* FILE PATH: Views/Officer/OrgTreasurerDashboard.cshtml *@
@{
    ViewData["Title"] = "Organization Treasury Dashboard";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";

    var totalFeesCollected = ViewBag.TotalFeesCollected ?? 0m;
    var totalFinesCollected = ViewBag.TotalFinesCollected ?? 0m;
    var pendingFees = ViewBag.PendingFees ?? 0m;
    var pendingFines = ViewBag.PendingFines ?? 0m;
    var totalCollections = ViewBag.TotalCollections ?? 0m;
    var sectionStats = ViewBag.SectionStats as List<dynamic> ?? new List<dynamic>();
}

@section Styles {
    <style>
        .stats-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 15px;
            padding: 24px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

            .stats-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            }

        .stats-icon {
            font-size: 3rem;
            margin-bottom: 12px;
        }

        .section-breakdown-card {
            background: rgba(255,255,255,0.05);
            border-left: 4px solid var(--gold-primary);
            padding: 16px;
            border-radius: 8px;
            margin-bottom: 12px;
            transition: transform 0.2s ease;
        }

            .section-breakdown-card:hover {
                transform: translateX(5px);
                background: rgba(255,255,255,0.08);
            }
    </style>
}

<!-- HEADER -->
<div class="glass-panel d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="glass-header-title">
            <i class="bi bi-bank2 me-2 text-gold"></i> Organization Treasury Dashboard
        </h4>
        <p class="mb-0 small text-muted">Complete financial overview for all students</p>
    </div>
    <div>
        <a href="@Url.Action("PaymentReminders", "Officer")" class="btn btn-warning">
            <i class="bi bi-megaphone me-1"></i> Post Reminder
        </a>
    </div>
</div>

<!-- QUICK ACCESS CARDS -->
<div class="row mb-4">
    <div class="col-md-6">
        <a href="@Url.Action("OrgFees", "Officer")" class="text-decoration-none">
            <div class="glass-panel p-4 h-100" style="transition: transform 0.2s ease, box-shadow 0.2s ease; cursor: pointer;">
                <div class="d-flex align-items-center">
                    <div class="me-4" style="background: rgba(16, 185, 129, 0.15); color: #10b981; width: 70px; height: 70px; display: flex; align-items: center; justify-content: center; border-radius: 15px;">
                        <i class="bi bi-cash-stack fs-1"></i>
                    </div>
                    <div>
                        <h5 class="text-white mb-1 fw-bold">Fees Management</h5>
                        <p class="text-muted mb-0 small">View all fees, create new fees, and track collections across all sections</p>
                    </div>
                    <div class="ms-auto">
                        <i class="bi bi-chevron-right text-gold fs-4"></i>
                    </div>
                </div>
            </div>
        </a>
    </div>
    <div class="col-md-6">
        <a href="@Url.Action("OrgFines", "Officer")" class="text-decoration-none">
            <div class="glass-panel p-4 h-100" style="transition: transform 0.2s ease, box-shadow 0.2s ease; cursor: pointer;">
                <div class="d-flex align-items-center">
                    <div class="me-4" style="background: rgba(239, 68, 68, 0.15); color: #ef4444; width: 70px; height: 70px; display: flex; align-items: center; justify-content: center; border-radius: 15px;">
                        <i class="bi bi-exclamation-triangle fs-1"></i>
                    </div>
                    <div>
                        <h5 class="text-white mb-1 fw-bold">Fines Management</h5>
                        <p class="text-muted mb-0 small">View all fines, create new fines, and monitor penalty collections</p>
                    </div>
                    <div class="ms-auto">
                        <i class="bi bi-chevron-right text-gold fs-4"></i>
                    </div>
                </div>
            </div>
        </a>
    </div>
</div>

<!-- REMITTANCE MANAGEMENT CARDS -->
<div class="row mb-4">
    <div class="col-md-6">
        <a href="@Url.Action("PendingRemittances", "Officer")" class="text-decoration-none">
            <div class="glass-panel p-4 h-100" style="transition: transform 0.2s ease, box-shadow 0.2s ease; cursor: pointer; background: linear-gradient(135deg, rgba(245, 158, 11, 0.15), rgba(245, 158, 11, 0.05)); border: 1px solid rgba(245, 158, 11, 0.4);">
                <div class="d-flex align-items-center">
                    <div class="me-4" style="background: rgba(245, 158, 11, 0.2); color: #fbbf24; width: 70px; height: 70px; display: flex; align-items: center; justify-content: center; border-radius: 15px;">
                        <i class="bi bi-hourglass-split fs-1"></i>
                    </div>
                    <div>
                        <h5 class="text-white mb-1 fw-bold">Pending Remittances</h5>
                        <p class="text-muted mb-0 small">Review and validate remittances from Class Treasurers</p>
                    </div>
                    <div class="ms-auto">
                        <i class="bi bi-chevron-right fs-4" style="color: #fbbf24;"></i>
                    </div>
                </div>
            </div>
        </a>
    </div>
    <div class="col-md-6">
        <a href="@Url.Action("ValidatedRemittances", "Officer")" class="text-decoration-none">
            <div class="glass-panel p-4 h-100" style="transition: transform 0.2s ease, box-shadow 0.2s ease; cursor: pointer; background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(16, 185, 129, 0.05)); border: 1px solid rgba(16, 185, 129, 0.3);">
                <div class="d-flex align-items-center">
                    <div class="me-4" style="background: rgba(16, 185, 129, 0.2); color: #34d399; width: 70px; height: 70px; display: flex; align-items: center; justify-content: center; border-radius: 15px;">
                        <i class="bi bi-check-all fs-1"></i>
                    </div>
                    <div>
                        <h5 class="text-white mb-1 fw-bold">Validated Remittances</h5>
                        <p class="text-muted mb-0 small">View history of all validated remittances</p>
                    </div>
                    <div class="ms-auto">
                        <i class="bi bi-chevron-right fs-4" style="color: #34d399;"></i>
                    </div>
                </div>
            </div>
        </a>
    </div>
</div>

<!-- STATISTICS CARDS -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="stats-card">
            <i class="bi bi-wallet-fill stats-icon text-success"></i>
            <h6 class="text-muted mb-1">Total Collections</h6>
            <h2 class="fw-bold text-success mb-0">₱@totalCollections.ToString("N2")</h2>
            <small class="text-muted">Fees + Fines</small>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stats-card">
            <i class="bi bi-receipt-cutoff stats-icon text-primary"></i>
            <h6 class="text-muted mb-1">Fees Collected</h6>
            <h2 class="fw-bold text-primary mb-0">₱@totalFeesCollected.ToString("N2")</h2>
            <small class="text-muted">From all students</small>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stats-card">
            <i class="bi bi-exclamation-octagon-fill stats-icon text-warning"></i>
            <h6 class="text-muted mb-1">Fines Collected</h6>
            <h2 class="fw-bold text-warning mb-0">₱@totalFinesCollected.ToString("N2")</h2>
            <small class="text-muted">From penalties</small>
        </div>
    </div>
    <div class="col-md-3">
        <div class="stats-card">
            <i class="bi bi-hourglass-split stats-icon text-danger"></i>
            <h6 class="text-muted mb-1">Total Pending</h6>
            <h2 class="fw-bold text-danger mb-0">₱@((pendingFees + pendingFines).ToString("N2"))</h2>
            <small class="text-muted">Outstanding balance</small>
        </div>
    </div>
</div>

<!-- CHARTS AND BREAKDOWN -->
<div class="row">
    <div class="col-md-8">
        <div class="glass-panel">
            <h5 class="glass-header-title mb-3">
                <i class="bi bi-pie-chart-fill me-2 text-gold"></i> Financial Overview
            </h5>
            <div style="position: relative; height: 400px;">
                <canvas id="financialChart"></canvas>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="glass-panel">
            <h5 class="glass-header-title mb-3">
                <i class="bi bi-list-check me-2 text-gold"></i> Quick Stats
            </h5>
            <div class="table-responsive">
                <table class="table table-sm text-white">
                    <tbody>
                        <tr>
                            <td><i class="bi bi-check-circle-fill text-success me-2"></i> Total Collected</td>
                            <td class="text-end fw-bold text-success">₱@totalCollections.ToString("N2")</td>
                        </tr>
                        <tr>
                            <td><i class="bi bi-hourglass-split text-warning me-2"></i> Total Pending</td>
                            <td class="text-end fw-bold text-warning">₱@((pendingFees + pendingFines).ToString("N2"))</td>
                        </tr>
                        <tr class="table-active" style="background-color: rgba(255, 215, 0, 0.1);">
                            <td class="text-white"><i class="bi bi-calculator-fill me-2" style="color: #FFD700;"></i> <strong>Grand Total</strong></td>
                            <td class="text-end fw-bold" style="color: #FFD700; font-size: 1.1rem;">₱@((totalCollections + pendingFees + pendingFines).ToString("N2"))</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <hr class="my-3" />

            <h6 class="text-uppercase small fw-bold text-muted mb-3">Collection Rate</h6>
            <div class="progress" style="height: 30px;">
                @{
                    var grandTotal = totalCollections + pendingFees + pendingFines;
                    var collectionRate = grandTotal > 0 ? (totalCollections / grandTotal * 100) : 0;
                }
                <div class="progress-bar bg-success" role="progressbar" style="width: @collectionRate%" aria-valuenow="@collectionRate" aria-valuemin="0" aria-valuemax="100">
                    @collectionRate.ToString("F1")%
                </div>
            </div>
        </div>
    </div>
</div>

<!-- MONTHLY COLLECTION TRENDS -->
<div class="row mt-4">
    <div class="col-12">
        <div class="glass-panel">
            <h5 class="glass-header-title mb-3">
                <i class="bi bi-graph-up me-2 text-gold"></i> Collection Trends
            </h5>

            <!-- Filter Tabs -->
            <ul class="nav nav-tabs mb-3" id="filterTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="quick-filter-tab" data-bs-toggle="tab" 
                            data-bs-target="#quick-filter" type="button" role="tab">
                        <i class="bi bi-lightning-charge me-1"></i> Quick Filter
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="month-filter-tab" data-bs-toggle="tab" 
                            data-bs-target="#month-filter" type="button" role="tab">
                        <i class="bi bi-calendar-month me-1"></i> By Month
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="daterange-filter-tab" data-bs-toggle="tab" 
                            data-bs-target="#daterange-filter" type="button" role="tab">
                        <i class="bi bi-calendar-range me-1"></i> Date Range
                    </button>
                </li>
            </ul>

            <!-- Tab Content -->
            <div class="tab-content mb-3" id="filterTabContent">
                <!-- Quick Filter Tab -->
                <div class="tab-pane fade show active" id="quick-filter" role="tabpanel">
                    <div class="row g-2 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label small text-white">Time Range</label>
                            <select id="quickRange" class="form-select form-select-sm">
                                <option value="lastWeek">Last Week</option>
                                <option value="lastMonth">Last Month</option>
                                <option value="last3Months" selected>Last 3 Months</option>
                                <option value="last6Months">Last 6 Months</option>
                                <option value="currentSemester">Current Semester</option>
                                <option value="fullYear">Full Academic Year</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white">Academic Year</label>
                            <select id="academicYear" class="form-select form-select-sm">
                                <option value="2025-2026" selected>2025-2026</option>
                                <option value="2024-2025">2024-2025</option>
                                <option value="2023-2024">2023-2024</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <button class="btn btn-sm w-100" onclick="applyFilter()" 
                                    style="background-color: #FFD700; color: #000; font-weight: bold; border: 2px solid #FFD700;">
                                <i class="bi bi-funnel me-1"></i> Apply Filter
                            </button>
                        </div>
                    </div>
                </div>

                <!-- By Month Tab -->
                <div class="tab-pane fade" id="month-filter" role="tabpanel">
                    <div class="row g-2 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label small text-white">Month</label>
                            <select id="monthSelect" class="form-select form-select-sm">
                                <option value="1">January</option>
                                <option value="2" selected>February</option>
                                <option value="3">March</option>
                                <option value="4">April</option>
                                <option value="5">May</option>
                                <option value="6">June</option>
                                <option value="7">July</option>
                                <option value="8">August</option>
                                <option value="9">September</option>
                                <option value="10">October</option>
                                <option value="11">November</option>
                                <option value="12">December</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white">Year</label>
                            <select id="yearSelect" class="form-select form-select-sm">
                                <option value="2026" selected>2026</option>
                                <option value="2025">2025</option>
                                <option value="2024">2024</option>
                                <option value="2023">2023</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <button class="btn btn-sm w-100" onclick="applyFilter()" 
                                    style="background-color: #FFD700; color: #000; font-weight: bold; border: 2px solid #FFD700;">
                                <i class="bi bi-funnel me-1"></i> Apply Filter
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Date Range Tab -->
                <div class="tab-pane fade" id="daterange-filter" role="tabpanel">
                    <div class="row g-2 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label small text-white">From Date</label>
                            <input type="date" id="fromDate" class="form-control form-control-sm" 
                                   value="@DateTime.Now.AddMonths(-1).ToString("yyyy-MM-dd")">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white">To Date</label>
                            <input type="date" id="toDate" class="form-control form-control-sm" 
                                   value="@DateTime.Now.ToString("yyyy-MM-dd")">
                        </div>
                        <div class="col-md-4">
                            <button class="btn btn-sm w-100" onclick="applyFilter()" 
                                    style="background-color: #FFD700; color: #000; font-weight: bold; border: 2px solid #FFD700;">
                                <i class="bi bi-funnel me-1"></i> Apply Filter
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Current Filter Label -->
            <div class="mb-2">
                <small class="text-muted">
                    <i class="bi bi-filter-circle me-1"></i> 
                    Showing: <span id="currentFilterLabel" class="text-gold fw-bold">Last 3 Months</span>
                </small>
            </div>

            <!-- Chart -->
            <div style="position: relative; height: 350px;">
                <canvas id="trendsChart"></canvas>
            </div>
        </div>
    </div>
</div>

<!-- PROGRAM-YEAR BREAKDOWN -->
<div class="row mt-4">
    <div class="col-12">
        <div class="glass-panel">
            <h5 class="glass-header-title mb-3">
                <i class="bi bi-diagram-3-fill me-2 text-gold"></i> Paid/Unpaid by Program-Year
                <button class="btn btn-sm btn-outline-light float-end" onclick="exportProgramYearData()">
                    <i class="bi bi-download me-1"></i> Export
                </button>
            </h5>
            @if (ViewBag.ProgramYearStats != null && ((IEnumerable<dynamic>)ViewBag.ProgramYearStats).Any())
            {
                @foreach (var stat in ViewBag.ProgramYearStats)
                {
                    var programYearTotal = (decimal)stat.TotalFees;
                    var programYearPaid = (decimal)stat.PaidFees;
                    var programYearUnpaid = programYearTotal - programYearPaid;

                    <div class="section-breakdown-card">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h6 class="mb-0 text-white">@stat.ProgramYear</h6>
                        </div>
                        <div class="row text-center mt-3">
                            <div class="col-4">
                                <small class="text-muted d-block">Total Expected</small>
                                <h6 class="text-white mb-0">₱@programYearTotal.ToString("N2")</h6>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">Paid</small>
                                <h6 class="text-success mb-0">₱@programYearPaid.ToString("N2")</h6>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">Unpaid</small>
                                <h6 class="text-danger mb-0">₱@programYearUnpaid.ToString("N2")</h6>
                            </div>
                        </div>
                        @{
                            var paidPercent = programYearTotal > 0 ? (programYearPaid / programYearTotal * 100) : 0;
                            var unpaidPercent = programYearTotal > 0 ? (programYearUnpaid / programYearTotal * 100) : 0;
                        }
                        <div class="progress mt-3" style="height: 30px; position: relative; background-color: rgba(220, 53, 69, 0.3);">
                            <div class="progress-bar bg-success d-flex align-items-center justify-content-center" role="progressbar" style="width: @paidPercent.ToString("F1")%">
                                @if (paidPercent > 5)
                                {
                                    <span class="text-white fw-bold small">@paidPercent.ToString("F1")% Paid</span>
                                }
                            </div>
                            @if (unpaidPercent > 5)
                            {
                                <span class="position-absolute text-white fw-bold small" style="right: 10px; top: 50%; transform: translateY(-50%);">
                                    @unpaidPercent.ToString("F1")% Unpaid
                                </span>
                            }
                        </div>
                    </div>
                }
            }
            else
            {
                <p class="text-center text-muted py-4">No program-year data available.</p>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Monthly Trends Line Chart
        const trendsCtx = document.getElementById('trendsChart').getContext('2d');
        const monthlyTrends = @Html.Raw(Json.Serialize(ViewBag.MonthlyTrends ?? new List<object>()));
        
        // Debug: Log the data
        console.log('Initial Monthly Trends Data:', monthlyTrends);
        console.log('Number of months:', monthlyTrends.length);
        
        // Global chart variable
        let trendsChart = null;
        
        // Initialize chart
        function initializeTrendsChart(data) {
            // Destroy existing chart if any
            if (trendsChart) {
                trendsChart.destroy();
            }

            // Check if we have data
            if (!data || data.length === 0) {
                // Show no data message
                const canvas = document.getElementById('trendsChart');
                const ctx = canvas.getContext('2d');
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                ctx.fillStyle = '#fff';
                ctx.font = '16px Arial';
                ctx.textAlign = 'center';
                ctx.fillText('No collection data available for the selected period', canvas.width / 2, canvas.height / 2);
                return;
            }

            // Debug: Log raw data first
            console.log('Raw data received:', data);
            console.log('First item:', data[0]);
            console.log('Property names:', data[0] ? Object.keys(data[0]) : 'no data');

            // Calculate max value for Y-axis scale
            const feesData = data.map(t => t.feesCollected || t.FeesCollected || 0);
            const finesData = data.map(t => t.finesCollected || t.FinesCollected || 0);
            const labels = data.map(t => t.month || t.Month || 'Unknown');
            const maxFees = Math.max(...feesData);
            const maxFines = Math.max(...finesData);
            const maxValue = Math.max(maxFees, maxFines);
            const suggestedMaxValue = maxValue > 0 ? maxValue * 1.2 : 100;

            console.log('Processed Chart Data:', {
                labels: labels,
                feesData: feesData,
                finesData: finesData,
                maxValue: maxValue,
                suggestedMax: suggestedMaxValue
            });

            trendsChart = new Chart(trendsCtx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [
                        {
                            label: 'Fees Collected',
                            data: feesData,
                            borderColor: '#3b82f6',
                            backgroundColor: 'rgba(59, 130, 246, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointRadius: 5,
                            pointHoverRadius: 8,
                            pointBackgroundColor: '#3b82f6',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2
                        },
                        {
                            label: 'Fines Collected',
                            data: finesData,
                            borderColor: '#f59e0b',
                            backgroundColor: 'rgba(245, 158, 11, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointRadius: 5,
                            pointHoverRadius: 8,
                            pointBackgroundColor: '#f59e0b',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top',
                            labels: { 
                                color: '#fff', 
                                font: { size: 14, weight: 'bold' },
                                padding: 20,
                                usePointStyle: true
                            }
                        },
                        tooltip: {
                            backgroundColor: 'rgba(0, 0, 0, 0.8)',
                            titleColor: '#fff',
                            bodyColor: '#fff',
                            borderColor: '#FFD700',
                            borderWidth: 1,
                            padding: 12,
                            callbacks: {
                                label: function(context) {
                                    return context.dataset.label + ': ₱' + context.parsed.y.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2});
                                }
                            }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            suggestedMax: suggestedMaxValue,
                            ticks: { 
                                color: '#fff',
                                font: { size: 12, weight: 'bold' },
                                padding: 10,
                                callback: function(value) {
                                    // Format large numbers with K/M suffix
                                    if (value >= 1000000) {
                                        return '₱' + (value / 1000000).toFixed(1) + 'M';
                                    } else if (value >= 1000) {
                                        return '₱' + (value / 1000).toFixed(1) + 'K';
                                    }
                                    return '₱' + value.toLocaleString('en-US', {minimumFractionDigits: 0, maximumFractionDigits: 0});
                                }
                            },
                            grid: { 
                                color: 'rgba(255, 255, 255, 0.15)',
                                drawBorder: true,
                                borderColor: 'rgba(255, 255, 255, 0.3)',
                                borderWidth: 2
                            }
                        },
                        x: {
                            display: true,
                            position: 'bottom',
                            ticks: { 
                                color: '#fff',
                                font: { size: 12, weight: 'bold' },
                                padding: 10,
                                maxRotation: 0,
                                minRotation: 0,
                                autoSkip: false
                            },
                            grid: { 
                                display: false,
                                drawBorder: true,
                                borderColor: 'rgba(255, 255, 255, 0.3)',
                                borderWidth: 2
                            }
                        }
                    }
                }
            });
        }

        // Apply Filter Function
        function applyFilter() {
            // Determine which tab is active
            const activeTab = document.querySelector('#filterTabs .nav-link.active').id;
            let params = {};
            
            if (activeTab === 'quick-filter-tab') {
                // Quick Filter mode
                params = {
                    filterType: 'quick',
                    quickRange: document.getElementById('quickRange').value,
                    academicYear: document.getElementById('academicYear').value
                };
            } else if (activeTab === 'month-filter-tab') {
                // By Month mode
                params = {
                    filterType: 'month',
                    month: document.getElementById('monthSelect').value,
                    year: document.getElementById('yearSelect').value
                };
            } else if (activeTab === 'daterange-filter-tab') {
                // Date Range mode
                const fromDate = document.getElementById('fromDate').value;
                const toDate = document.getElementById('toDate').value;
                
                // Validation
                if (!fromDate || !toDate) {
                    alert('Please select both From Date and To Date');
                    return;
                }
                
                if (new Date(toDate) < new Date(fromDate)) {
                    alert('To Date must be after From Date');
                    return;
                }
                
                params = {
                    filterType: 'daterange',
                    fromDate: fromDate,
                    toDate: toDate
                };
            }
            
            // Show loading indicator
            document.getElementById('currentFilterLabel').innerHTML = '<i class="spinner-border spinner-border-sm me-1"></i> Loading...';
            
            // Make AJAX call
            fetch('/Officer/GetMonthlyTrends?' + new URLSearchParams(params))
                .then(response => response.json())
                .then(result => {
                    console.log('Filter Result:', result);
                    
                    if (result.success) {
                        // Update filter label
                        document.getElementById('currentFilterLabel').textContent = result.filterLabel;
                        
                        // Update chart
                        initializeTrendsChart(result.data);
                    } else {
                        alert('Error: ' + result.message);
                        document.getElementById('currentFilterLabel').textContent = 'Error loading data';
                    }
                })
                .catch(error => {
                    console.error('Error fetching trends:', error);
                    alert('Failed to load data. Please try again.');
                    document.getElementById('currentFilterLabel').textContent = 'Error';
                });
        }

        // Initialize chart with default data on page load
        initializeTrendsChart(monthlyTrends);

        // Financial Doughnut Chart
        const ctx = document.getElementById('financialChart').getContext('2d');
        new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: ['Paid Fees', 'Paid Fines', 'Unpaid Fees', 'Unpaid Fines'],
                datasets: [{
                    data: [@totalFeesCollected, @totalFinesCollected, @pendingFees, @pendingFines],
                    backgroundColor: ['#3b82f6', '#f59e0b', '#ef4444', '#dc2626'],
                    borderWidth: 2,
                    borderColor: 'rgba(255, 255, 255, 0.1)'
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: {
                            color: '#fff',
                            padding: 20,
                            font: { size: 14 }
                        }
                    },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                return context.label + ': ₱' + context.parsed.toLocaleString('en-US', {minimumFractionDigits: 2});
                            }
                        }
                    }
                }
            }
        });
    </script>
}
