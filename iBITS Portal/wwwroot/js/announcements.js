document.addEventListener('DOMContentLoaded', function () {
    console.log("📢 Announcements Manager Initialized");

    // This file is included for consistency.
    // The delete confirmation is currently handled by an inline 'onsubmit' event in the CSHTML.
    // More complex client-side logic can be added here in the future if needed.
});